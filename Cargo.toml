[package]
name = "audio-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
backend = { path = "backend", package = "audio-backend" }
smart-repl = { path = "smart-repl" }
log = "0.4"
simplelog = "0.12"

[lints.clippy]
pedantic = "warn"
